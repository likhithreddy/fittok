{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-09-29/server.schema.json",
  "name": "io.github.likhithreddy/fittok",
  "title": "fittok",
  "description": "Retrieve only the relevant source code for a codebase question within a token budget, so the model answers from a focused slice instead of whole files. Less input = fewer tokens, lower cost, faster answers. Supports Python, JS/JSX/TS/TSX, Java, Go, Rust.",
  "version": "0.5.0",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "fittok",
      "version": "0.5.0",
      "transport": { "type": "stdio" }
    }
  ]
}
